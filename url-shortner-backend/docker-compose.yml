services:
  backend:
    build: .
    container_name: url-shortener-backend

    ports:
      - "8080:8080"

    env_file:
      - .env

    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: url-shortener-redis

    ports:
      - "6380:6379"

    restart: unless-stopped